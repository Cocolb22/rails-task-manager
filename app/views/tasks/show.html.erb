<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Edit the task", edit_task_path %>

<%= link_to "Return to your list", root_path %>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>